require("visual.colorschemes")

local colors = ROSE_PINE

other_colors = {
  border = colors.black3,
  font = colors.white,
  bg = colors.black1,
}

---{{{ Widget Colors
app_launcher_colors = {
  fg = colors.black3,
  fg_icon = colors.black3,
  bg = colors.red1,
  border = colors.red2,
}

battery_colors = {
  fg = colors.black3,
  fg_icon = colors.black3,
  bg = colors.orange1,
  bg_icon = colors.orange2,
  border = colors.orange2,
}

time_colors = {
  fg = colors.black3,
  fg_icon = colors.cyan1,
  bg = colors.cyan1,
  bg_icon = colors.cyan2,
  border = colors.cyan2,
}

date_colors = {
  fg = colors.black3,
  fg_icon = colors.black3,
  bg = colors.purple1,
  bg_icon = colors.purple2,
  border = colors.purple2,
}

tray_colors = {
  fg = colors.black3,
  fg_icon = colors.black3,
  bg = colors.black3,
  border = colors.black3,
}
---}}}

taglist_colors = {
  bg = colors.peach2,
  bg_focus = colors.peach1,
  bg_urgent = colors.black3,
  fg = colors.peach1,
  fg_focus = colors.black3,
  fg_occupied = colors.black3,
  fg_urgent = colors.red1,
  border = colors.red2,
  hover = colors.gray1,
}

tasklist_colors = {
  fg = colors.black3,
  fg_focus = colors.black3,
  fg_minimize = colors.gray2,
  bg = colors.black3,
  bg_focus = colors.orange2,
  bg_minimize = colors.red2,
  border = colors.orange2,
}

titlebar_colors = {
  border = colors.black3,
  border_focus = colors.peach2,
}

bar_colors = {
  bg = colors.black3,
  border = colors.black3,
}
